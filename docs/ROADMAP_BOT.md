# Что добавить в бота «Молоток» — пошагово

**Статус:** пункты 1–10 реализованы (FAQ, акции, имя в приветствии, кнопки в контактах, несколько админов, rate limit, каталог по кнопкам, логирование в файл, /stats, /broadcast, /done, уведомление клиенту через /done).

Ниже — исходные рекомендации по доработкам с приоритетом и пояснениями.

---

## 1. Раздел «Частые вопросы» (FAQ)

**Зачем:** В README на GitHub указано «Ответы на частые вопросы», но в боте этого нет. Клиенты часто спрашивают одно и то же (доставка, оплата, возврат) — готовые ответы разгрузят менеджера.

**Что сделать:**
- Добавить кнопку «❓ Частые вопросы» в главное меню (в `handlers/start.py`).
- В `handlers/callbacks.py` обработать `callback_data='faq'`: показать сообщение с 3–5 вопросами и ответами (например: «Есть доставка?» — «По Воронежу от 300 руб.»; «Какая оплата?» — «Наличные, карта»; «Можно вернуть товар?» — «По согласованию с менеджером»).

**Сложность:** низкая. Только текст и одна кнопка.

---

## 2. Раздел «Акции и скидки»

**Зачем:** В описании магазина есть «Акции и скидки» — отдельная кнопка увеличивает вовлечённость и даёт место для промо без изменения каталога.

**Что сделать:**
- Кнопка «🏷️ Акции» в главном меню.
- Обработчик выводит текст с текущими акциями (например: «Скидка 10% на краску при заказе от 5 000 ₽»). Текст можно хранить в одном месте (константа или короткий файл), чтобы менять без правки кода.

**Сложность:** низкая.

---

## 3. Несколько администраторов (пересылка заказов нескольким людям)

**Зачем:** Сейчас заказы уходят только одному ID. Если нужно, чтобы получали и Алексей, и ты (или ещё менеджеры), одного `ADMIN_CHAT_ID` недостаточно.

**Что сделать:**
- В `.env` задать список ID через запятую: `ADMIN_CHAT_IDS=7282606357,516907600`.
- В `config.py` читать эту переменную и разбивать по запятой в список чисел.
- В `handlers/orders.py` в цикле вызывать `bot.send_message(admin_id, admin_text, ...)` для каждого ID. Ошибку для одного админа логировать, но не прерывать отправку остальным.

**Сложность:** средняя. Нужно аккуратно обработать пустые значения и невалидные ID.

---

## 4. Имя пользователя в приветствии

**Зачем:** Обращение по имени («Добрый день, Алексей!») делает общение более личным.

**Что сделать:**
- В обработчике `/start` взять `message.from_user.first_name` (или `message.from_user.username` при отсутствии имени).
- Подставить в приветствие: «Добро пожаловать, {имя}!» или «Добро пожаловать в магазин «Молоток», {имя}!».

**Сложность:** низкая.

---

## 5. Защита от спама (rate limit)

**Зачем:** Если кто-то начнёт слать десятки сообщений подряд, бот будет логировать и пересылать всё админам и может упереться в лимиты Telegram.

**Что сделать:**
- Запомнить время последнего «заказа» по `user_id` (словарь или простая БД).
- Если от того же пользователя новое текстовое сообщение (заказ) пришло раньше чем через N секунд (например, 30–60) — не считать заказом, отправить: «Подождите минуту перед следующим заказом» и не пересылать админу.

**Сложность:** средняя. Нужно хранить состояние (в памяти или в SQLite).

---

## 6. Каталог по кнопкам (категории → подкатегории)

**Зачем:** Сейчас каталог — один большой текст. Удобнее: «Краски» / «Инструменты» / «Крепёж» — при нажатии показывается подсписок или текст по категории.

**Что сделать:**
- При нажатии «Каталог» показывать не текст, а кнопки: «Краски и лаки», «Инструменты», «Крепёж», «Сад и огород».
- У каждой кнопки свой `callback_data`, например `catalog_paint`, `catalog_tools` и т.д.
- В обработчике callback по префиксу `catalog_` показывать соответствующий текст (или следующий уровень кнопок).

**Сложность:** средняя. Чуть больше кнопок и условий в `callbacks.py`.

---

## 7. Команды для админа (статистика, рассылка)

**Зачем:** Админу удобно из бота узнавать количество заказов за день/неделю или отправить всем клиентам сообщение об акции.

**Что сделать:**
- Проверять, что `message.from_user.id` входит в список `ADMIN_CHAT_IDS` (или равен `ADMIN_CHAT_ID`).
- Команда `/stats`: из БД (таблица `orders`) посчитать заказы за сегодня/неделю и отправить админу короткую сводку.
- Команда `/broadcast <текст>`: отправить сообщение всем `user_id`, которые когда-либо писали боту (их можно хранить в отдельной таблице или собирать из `orders`). Использовать осторожно, чтобы не нарушать правила Telegram.

**Сложность:** средняя–высокая. Нужны запросы к БД и аккуратная работа с рассылкой.

---

## 8. Уведомление клиенту о статусе заказа

**Зачем:** Сейчас клиент только видит «Заказ принят!». Если менеджер обработал заказ или позвонил — клиенту полезно получить короткое уведомление.

**Что сделать:**
- Админ-команда вида `/done <id_заказа>` или ответ (reply) админа на пересланное сообщение о заказе с текстом «готово».
- Бот по `order_id` находит `chat_id` клиента в БД и отправляет ему сообщение: «Ваш заказ №X принят в работу» или «Менеджер свяжется с вами в ближайшее время» (шаблон на выбор).

**Сложность:** высокая. Нужна связь «заказ → chat_id», хранение заказов с id и безопасная проверка, что команду вызвал админ.

---

## 9. Кнопка «Позвонить» и «Написать» в контактах

**Зачем:** В Telegram можно открыть звонок или чат по номеру/username — меньше копирования, быстрее действие.

**Что сделать:**
- В блоке контактов добавить инлайн-кнопки: `url` на `tel:+79585094499` (звонок) и при наличии — кнопку «Написать в Telegram» с ссылкой на менеджера (t.me/username) или на группу поддержки.

**Сложность:** низкая. В `callbacks.py` при выводе контактов добавить `InlineKeyboardButton` с `url=...`.

---

## 10. Логирование в файл (не только заказы)

**Зачем:** Сейчас в `journalctl` видны ошибки и вывод print. При падении или странном поведении проще разбираться, если есть файл лога с датой и уровнем (info/error).

**Что сделать:**
- Подключить модуль `logging`: один файл `data/bot.log`, ротация по размеру или по дням. В `main.py` и в обработчиках вызывать `logging.info(...)` / `logging.exception(...)` вместо или в дополнение к `print`.
- Ошибку «Не удалось отправить заказ админу» писать через `logging.exception`, чтобы в файле был полный traceback.

**Сложность:** низкая. Настройка один раз в `main.py` или отдельном модуле.

---

## Порядок внедрения (рекомендуемый)

| Шаг | Что добавить | Зачем сначала |
|-----|----------------|----------------|
| 1 | FAQ | Быстро, полезно клиентам, уже обещано в описании |
| 2 | Акции | То же: обещано, мало кода |
| 3 | Имя в приветствии | Очень просто, сразу «теплее» |
| 4 | Кнопки «Позвонить» / «Написать» в контактах | Удобство в один клик |
| 5 | Несколько админов | Если нужно получать заказы не только одному человеку |
| 6 | Rate limit на заказы | Защита от спама и лишних уведомлений |
| 7 | Каталог по кнопкам | Улучшение навигации |
| 8 | Логирование в файл | Удобнее отладка и поддержка |
| 9 | Админ-команды (/stats, /broadcast) | По желанию, когда понадобится аналитика или рассылка |
| 10 | Уведомление клиенту о статусе заказа | Более сложный сценарий, после стабилизации остального |

Если скажешь, с какого пункта начать (например, FAQ + Акции + имя), могу расписать конкретные изменения в коде по файлам.
